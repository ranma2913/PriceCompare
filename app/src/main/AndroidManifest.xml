<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.ranma2913.pricecompare"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat">

        <!-- Splash screen -->
        <activity
            android:name=".SplashScreenActivity_"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity
            android:name=".MainActivity_"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden|adjustPan">
        </activity>
        <activity
            android:name=".EnterItemActivity_"
            android:label="@string/title_activity_enter_item"
            android:parentActivityName=".MainActivity_"
            android:windowSoftInputMode="stateVisible">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ranma2913.pricecompare.MainActivity_"/>
        </activity>
    </application>

</manifest>
